# Production / shared-hosting build (eduaitor.com)
# App is deployed under /admin/ because hosting keeps a separate site at domain root.

VITE_API_URL=https://eduaitor-api.onrender.com/api
VITE_APP_URL=https://eduaitor.com
VITE_BASE=/admin/
